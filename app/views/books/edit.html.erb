<div class="container">
	<div class="row">
		<div class="col-lg-3"></div>
		<div class="col-lg-6">
		<h2>Editing Book</h2>

		<% if @book.errors.any? %>
		    <div class="error_explanation" >
		      <h2><%= @book.errors.count %>error prohibited this obj from being saved:</h2>
		        <ul>
		          <% @book.errors.full_messages.each do |message| %>
		          <li><%= message %></li>
		          <% end %>
		        </ul>
		    </div>
		  <% end %>
		<div>

		<form class="edit_book" action="/books/<%= @book.id %>" id="edit_book_<%= @book.id %>" accept-charset="UTF-8" method="post">

			<input name="utf8" type="hidden" value="" >
			<input type="hidden" name="_method" value="patch" />
			<input type="hidden" name="book[user_id]" value="<%= @book.user_id %>" />

			<div class="form-group">
				<label form="name" class="control-label" for="usage1input2">Title</label>
				<br>
				<input type="text" value="<%= @book.title %>" name="book[title]" id="book_title" class="form-control input-sm">
			</div>
			<div class="form-group">
				<label form="title" lass="control-label" for="usage1input2">opinion</label>
				<br>
				<textarea name="book[body]" id="book_body" class="form-control input-lg"><%= @book.body %></textarea>
			</div>
				<input type="submit" name="commit" value="Create Book" class="btn btn-primary col-lg-12">
		</form>


		<div class="w-100"></div>
		<p class="col-lg-10">
		<%= link_to "Show", book_path %>
		|
		<%= link_to "Back", books_path %>
		</p>
		</div>

	</div>
  <div class="col-lg-3"></div>
</div>
</div>

