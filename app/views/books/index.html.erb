<h2>User info</h2>
<p>name</p>
<p>introduction</p>
<p>編集</p>


<h2>Books</h2>
	<table>
		<thead>
			<tr>
				<th></th>
				<th>Title</th>
				<th>opinion</th>
			</tr>
		</thead>
		<tbody>
			<% @books.each do |book| %>
				<tr>
					<td></td>
					<td><%= link_to book.title,book_path(book.id) %>
					</td>
					<td><%= book.body %></td>
				</tr>
			<% end %>
		   </tbody>
		</table>


<h2>New boook</h2>
<%= form_for(@book) do |f| %>
	<label>Title<br>
		<%= f.text_field :title %>
	</label>
	<br>
	<label>Opinion<br>
		<%= f.text_area :body %>
	</label>
	<br>
	<%= f.submit "Create Book" %>
<% end %>